{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "packages/site/.next",
  "installCommand": "npm install && cd packages/site && npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "packages/site/$1"
    }
  ]
}
